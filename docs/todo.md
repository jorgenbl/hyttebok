Lets create a plan for new functionality:

* Now we can edit raw md format. can we add some functionality to the editor to add checklist, bullet points, numbered lists (like the image buttons?) I would like it to be simple for a user to write the documentation. (when in a list, next bullet automatically appears when pressing enter, and if back(erase) one goes out of the bullets, checklists and numbered lists)
* Spice up the frontpage a bit with a cool cabin design
* help section and guided tour?
* Add some spacing between the headers and content. Maybe lines between each sections.
* Add support for adding multiple ai-providers/models to select for different purposes:
    * Add voice mode, with whisper ai, or local whisper models if possible.
    * Add AI support for image generation.
    * Current AI implementation
